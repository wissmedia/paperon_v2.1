<div class="modal" id="modalMenuAkun">
  <div class="modal-content">
    <div class="modal-kepala">
      <span class="close">&times;</span>
    </div>
    <% if(user){ %>
      <div class="modal-badan">
        <div class="profile" id="username">
          <div class="dpAkun">
            <div>
              <img src="<%= user.image %>" width="50" height="50" alt="User Image">
            </div>
            <h1>
              <%= user.displayName %>
            </h1>
            <h3>
              <%= user.email %>
            </h3>
          </div>
        </div>
        <a href="/auth/logout">
          <div class="list-menu-container" id="logout">
            <i class="fas fa-sign-out-alt"></i>
            <span class="label-list">
              Logout
            </span>
          </div>
        </a>
        <% if (user.role=='author' ) { %>
          <a href="/author">
            <div class="list-menu-container" id="author">
              <i class="fas fa-user-tie"></i>
              <span class="label-list">
                Author Panel
              </span>
            </div>
          </a>
          <a href="/respondent">
            <div class="list-menu-container" id="author">
              <i class="fas fa-pencil-alt"></i>
              <span class="label-list">
                Respondent Panel
              </span>
            </div>
          </a>
          <% } %>
            <% if (user.email==process.env.ADMIN_EMAIL) { %>
              <a href="/admin">
                <div class="list-menu-container" id="admin">
                  <i class="fas fa-user-shield"></i>
                  <span class="label-list">
                    Admin Panel
                  </span>
                </div>
              </a>
              <% } %>
      </div>
      <% }else{ %>
        <div class="form fadeIn">
          <div class="logo-form fadeInAtas">
            <img src="/logo/72.png" width="50" height="50" alt="Logo">
            <h3>
              <%= appNames.title %>
            </h3>
          </div>
          <div class="fadeIn">
            <h4>
              <%= appNames.subtitle %>
            </h4>
          </div>
          <br>
          <a href="/auth/google">
            <button class="tombol-md bunder fadeInBawah">
              <i class="fab fa-google left"></i> Signin with Google
            </button>
          </a>
        </div>
        <% } %>

  </div>
</div>